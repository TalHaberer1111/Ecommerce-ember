<h2>Shopping Cart</h2>

{{#each shoppingCart.items as |item|}}
  {{item.title}} -- ${{item.cost}}
  <button class="btn btn-default inline" {{action 'removeFromCart' item}}>Remove from Cart</button>
{{/each}}
